[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ir-test-task"
version = "0.1.0"
description = "Test task for Internet Revolution company"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [ { name = "Egor", email = "lotus9200@gmail.com" } ]
dependencies = [
    "pandas>=1.3.0",
    "openpyxl>=3.0.0"
]