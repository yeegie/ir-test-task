### 1. Создайте и активируйте виртуальное окружение

``` python
python -m venv .venv
```

Windows
``` bash
.\.venv\Scripts\activate
```

Linux
``` bash
source .venv/bin/activate
```
---

### 2. Установка зависимостей

``` bash
pip install .
```

---

### 3. Убедитесь что файл с токеном лежит в корневой директории

```
ir-test-task/
│
├── token.txt
├── data/
├── src/
├── pyproject.toml
└── ...
```

Внутри файл должен выглядеть так


```
0000000000:AAHQDE9UXsadAWDAWSDwadwa
```

---

### 4. Запуск приложения

``` bash
run_app
```

---

### Пояснение
1. В соответствии с ТЗ, группировку файлов реализовал при помощи декоратора заместо стейтов, по истечению пяти секунд происходит обработка файлов, если файлов больше чем один, в конце выводится информация о том, сколько файлов было обработано
2. Насколько возможно упросил и опустил лишние детали, чтобы код соответствовал kiss